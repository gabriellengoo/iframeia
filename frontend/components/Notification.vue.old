<!-- <template>
  <div class="notificationcont">
    <button>
      <div class="notification mobileonly">
        <div class="topnoti font-light flex items-center">
          <div class="flex items-center">
            <SvgMessages></SvgMessages>
            <p class="pl-2">Messages</p>
          </div>
          <div>
            <p class="pl-2">now</p>
          </div>
        </div>
        <div class="botnoti mobileonly">
              <div class="flex items-center">
                  <ProfilePicture />
                  <div class="topline">
                    <div class="font-bold toplinein">Web Dev ( The Internet Arcitect )<p class="pl-2 font-light text-[grey]">now</p></div>
                    <p class="font-light">Hey, Check your notes app!</p>
                  </div>
              </div>
        </div>

      </div>
    </button>
  </div>
</template>
  

<script>
import ProfilePicture from '~/components/ProfilePicture.vue';

export default {
  components: {
    ProfilePicture,
  },
};
</script>


  
<style scoped>

</style>
   -->


   <template>
    <div>
      <div class="notificationcont" :class="{ 'slide-in': isVisible }">
        <button @click="closeNotification">
          <div v-if="isVisible" class="notification">
            <!-- desk -->
            <!-- <div class="topnoti deskonly font-light flex items-center">
              <div class="flex items-center">
                <SvgMessages></SvgMessages>
                <p class="pl-2">Messages</p>
              </div>
              <div><p class="pl-2">now</p></div>
            </div>
  
            <div class="botnoti deskonly">
              <p class="font-bold">Web Dev ( The Internet Arcitect )</p>
              <p class="font-light">Hey, Check your notes app !</p>
            </div> -->
            <div class="botnoti">
              <div class="flex items-center">
                <ProfilePicture />
                <div class="topline">
                  <div class="font-bold toplinein">
                    Web Dev ( The Internet Arcitect )
                    <p class="pl-2 font-light text-[grey]">now</p>
                  </div>
                  <p class="font-light">Hey, Check your notes app!</p>
                </div>
              </div>
            </div>
            <!-- desk -->
  
            <!-- mobile -->
            <!-- <div class="botnoti mobileonly">
              <div class="flex items-center">
                <ProfilePicture />
                <div class="topline">
                  <div class="font-bold toplinein">
                    Web Dev ( The Internet Arcitect )
                    <p class="pl-2 font-light text-[grey]">now</p>
                  </div>
                  <p class="font-light">Hey, Check your notes app!</p>
                </div>
              </div>
            </div> -->
            <!-- mobile -->
          </div>
        </button>
      </div>
    </div>
  </template>
  
  <script>
  import ProfilePicture from "~/components/ProfilePicture.vue";
  
  export default {
    components: {
      ProfilePicture,
    },
    data() {
      return {
        isVisible: false,
        notificationText: "New Notification!",
      };
    },
    methods: {
      showNotification() {
        this.isVisible = true;
      },
      closeNotification() {
        this.isVisible = false;
        // Slide out the notification by setting a delay
        setTimeout(() => {
          this.$refs.notificationcont.style.top = "-100%";
        }, 50); // Adjust the delay as needed
      },
    },
    mounted() {
      // Show the notification automatically on mount
      setTimeout(() => {
        // Show the notification automatically after the delay
        this.showNotification();
  
        // Automatically close the notification after another delay
        setTimeout(() => {
          this.closeNotification();
          // time out
        }, 10000); // Adjust the delay as needed
        // time in
      }, 5000);
    },
  };
  </script>
  
  <style scoped></style>
  